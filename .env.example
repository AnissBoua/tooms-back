NODE_ENV=development
APP_PORT=3000

# DATABASE
DB_HOST=localhost
DB_PORT=3306
DB_USER=""
DB_PASS=""
DB_NAME="tooms"

# When "true", wipes all conversations/users/messages every 24h and reseeds
# the two demo accounts. Only enable this on the public demo deployment.
DEMO_RESET=false